export enum EHttpStatus {
  OK = 200,
  INTERNAL_SERVER_ERROR = 500,
  CREATED = 201,
  CONFLICT = 409,
  NOT_FOUND = 404,
  UNAUTHORIZED = 401,
  TOO_MANY_REQUESTS = 429,
  BAD_REQUEST = 400,
  FORBIDDEN = 403
}
